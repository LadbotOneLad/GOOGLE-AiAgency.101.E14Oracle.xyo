#include <M5Atom.h>
#include <WiFi.h>
#include <HTTPClient.h>
const char* ssid="b";const char* password="12345678";const char* hostPath="http://localhost:8080";const char* namespaceAnchor="aiagency101.xyo";float ax,ay,az;void setup(){M5.begin(true,false,true);delay(50);M5.dis.drawpix(0,0xf00000);WiFi.begin(ssid,password);while(WiFi.status()!=WL_CONNECTED){delay(500);};M5.IMU.Init();M5.dis.drawpix(0,0x0000f0);}void loop(){M5.update();M5.IMU.getAccelData(&ax,&ay,&az);float mt=abs(ax)+abs(ay)+abs(az);if(M5.Btn.wasPressed()||mt>2.5){if(WiFi.status()==WL_CONNECTED){M5.dis.drawpix(0,0xffffff);HTTPClient http;http.begin(hostPath);http.addHeader("Content-Type","text/plain");unsigned long ht=millis();String pl="M5_ATOM_WITNESS_TRIGGER|ID="+String(namespaceAnchor)+"|CLOCK="+String(ht)+"|X:"+String(ax)+"|Y:"+String(ay)+"|Z:"+String(az);int res=http.POST(pl);http.end();if(res>0){M5.dis.drawpix(0,0x00ff00);}else{M5.dis.drawpix(0,0xffaa00);}delay(500);M5.dis.drawpix(0,0x0000f0);}}}delay(50);}
